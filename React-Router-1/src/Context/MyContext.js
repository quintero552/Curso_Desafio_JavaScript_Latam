// Pirmero importamos desde React una destructuracion de createContext import { createContext} from "react"
import { createContext} from "react"

const MyContext = createContext({}) // Creamos la variable que tendra el contexto

export default MyContext
